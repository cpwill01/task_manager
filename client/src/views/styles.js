import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  fullGrid: {
    position: "absolute",
    paddingLeft: "2rem",
    paddingRight: "2rem",
  },
  gridItem: {
    paddingTop: 0,
  },
}));
